import {

} from '../constants/actionTypes';

const defaultState = {

};
export default function class2 (state = defaultState, action) {
  switch (action.type) {
    default:
      return state
  }
}


