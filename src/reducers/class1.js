import {

} from '../constants/actionTypes';

const defaultState = {

};
export default function class1 (state = defaultState, action) {
  switch (action.type) {
    default:
      return state
  }
}


